// =================================================================
// 导入模板
// =================================================================
#import "template/lib.typ": *

// =================================================================
// 封面设置
// =================================================================
#cover-design(
  // 封面标题（支持使用 \ 换行）
  title: [
    XXXXXXXXXXXXXXX\
    XXXXXX项目\
    中期检查报告
  ],

  // 信息字段列表
  info-items: (
    ("项目名称：", "项目名称第一行"),
    ("",           "第二行"),  // 标签留空，模拟换行的第二行
    ("项目编号：", "0000000"),
    ("起止时间：", "2024.11-2026.11"),
    ("分承研单位：", "XX大学"),
    ("项目负责人：", "姓 名"),
    ("编制日期：", "二〇二五年十月二十日"),
  )
)

// =================================================================
// 应用正文样式
// =================================================================
#show: report-style

// =================================================================
// 正文内容
// =================================================================

// --- 目录 ---
#align(center)[
  #outline(
    title: [目#hide("占位")录],
    depth: 2,
    indent: 0em
  )
]
#pagebreak()

// --- 启用页码（从引言开始编制页码）---
#show: enable-page-number

// --- 引言（无编号章节）---
#unnumbered-chapter("引言")
#pagebreak()

// --- 一级标题 ---
= 系统概述

这是正文内容。默认宋体，首行缩进 2em，两端对齐。行距已经设置为最小值 18 磅。当文字足够多的时候，你会看到它是自动换行且两端对齐的。

// --- 二级标题 ---
== 研究背景

这是二级标题下的正文。

// --- 三级标题 ---
=== 特定问题

这是三级标题下的正文。

// --- 插入表格示例 ---
#figure(
  caption: "系统参数定义表",
  kind: table,
  // 使用 table-content 包裹表格内容，应用表文样式（小五+单倍行距）
  table-content[
    #table(
      columns: (1fr, 1fr, 1fr),
      align: center + horizon,
      [参数名], [符号], [数值],
      [大气密度], [$rho$], [$1.225$],
      [温度], [$T$], [$288.15$],
    )
  ]
)

// --- 插入图片示例 ---
#figure(
  caption: "系统架构图",
  // 使用 image-content 包裹图片
  image-content[
    // #image("test.png", width: 80%)  // 实际使用时解开注释
    #rect(width: 80%, height: 4cm, fill: luma(220))[示意图占位符]
  ]
)

// --- 四级标题 ---
==== 细节分析

这是四级标题下的正文。

#pagebreak()

// --- 结论（无编号章节）---
#unnumbered-chapter("结论")