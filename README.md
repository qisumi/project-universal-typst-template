# Typst 通用报告模板

[![Typst](https://img.shields.io/badge/Typst-239DAD?style=flat&logo=typst&logoColor=white)](https://typst.app/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

一个结构化、易维护的 Typst 中文报告模板，适用于项目报告、技术文档等场景。

## 📁 项目结构

```
.
├── main.typ              # 主文档（使用示例）
├── fonts/                # 字体文件夹
└── template/             # 模板文件夹
    ├── lib.typ           # 统一入口
    ├── config.typ        # 全局配置（字体、字号、颜色等）
    ├── utils.typ         # 工具函数
    ├── cover.typ         # 封面模块
    └── report.typ        # 正文样式模块
```

## ✨ 特性

- **模块化设计**：各功能模块独立，便于维护和扩展
- **统一配置**：集中管理字体、字号、颜色等配置项
- **中文优化**：支持中文字体，符合中文排版规范
- **丰富样式**：
  - 四级标题自动编号
  - 图表自动编号和标题
  - 目录自动生成
  - 规范的页边距和行距

## 🚀 快速开始

### 1. 基本使用

```typst
// 导入模板
#import "template/lib.typ": *

// 设置封面
#cover-design(
  title: [你的报告标题],
  info-items: (
    ("项目名称：", "项目名称"),
    ("作者：", "作者姓名"),
    ("日期：", "2025年12月1日"),
  )
)

// 应用正文样式
#show: report-style

// 目录
#align(center)[
  #outline(title: [目录], depth: 2, indent: 0em)
]
#pagebreak()

// 正文内容
= 一级标题
正文内容...

== 二级标题
正文内容...

=== 三级标题
正文内容...
```

### 2. 创建封面

```typst
#cover-design(
  // 标题（支持使用 \ 换行）
  title: [
    这是第一行标题\
    这是第二行标题
  ],
  
  // 信息字段列表
  info-items: (
    ("项目名称：", "项目名称"),
    ("项目编号：", "12345"),
    ("作者：", "张三"),
    ("日期：", "2025年12月1日"),
  )
)
```

### 3. 插入图片

```typst
#figure(
  caption: "图片标题",
  image-content[
    #image("path/to/image.png", width: 80%)
  ]
)
```

### 4. 插入表格

```typst
#figure(
  caption: "表格标题",
  kind: table,
  table-content[
    #table(
      columns: (1fr, 1fr, 1fr),
      align: center + horizon,
      [列1], [列2], [列3],
      [数据1], [数据2], [数据3],
    )
  ]
)
```

### 5. 无编号章节

```typst
// 用于引言、结论等不需要编号的章节
#unnumbered-chapter("引言")
```

## 📝 配置说明

### 字体配置

在 `template/config.typ` 中可以修改字体：

```typst
#let fonts = (
  song: ("SimSun",),           // 宋体
  hei: ("SimHei",),            // 黑体
  fangsong: ("FangSong",),     // 仿宋
  // ...
)
```

### 字号配置

标准的 Word 字号已预定义：

```typst
#let font-sizes = (
  yihao: 26pt,      // 一号
  sanhao: 16pt,     // 三号
  sihao: 14pt,      // 四号
  xiaosi: 12pt,     // 小四
  xiaowu: 9pt,      // 小五
  // ...
)
```

### 页面配置

可在 `config.typ` 中调整页边距等：

```typst
#let page-config = (
  margin-top: 3cm,
  margin-bottom: 2.5cm,
  margin-left: 3.17cm,
  margin-right: 3.17cm,
)
```

## 🛠️ 高级功能

### 自定义标题样式

修改 `template/config.typ` 中的 `heading-config`：

```typst
#let heading-config = (
  numbering: "1.1.1.1",  // 编号格式
  level1: (
    font: fonts.hei-mix,
    size: font-sizes.sihao,
    // ...
  ),
  // ...
)
```

### 使用工具函数

```typst
// 垂直间距
#vspace(2cm)

// 水平分隔线
#horizontal-line(width: 50%, thickness: 1pt)

// 居中文本
#centered[居中的内容]
```

## 📦 依赖字体

模板需要以下中文字体（通常 Windows 系统自带）：

- SimSun（宋体）
- SimHei（黑体）
- FangSong（仿宋）

字体文件已包含在 `fonts/` 目录中。

## 🔧 编译

使用 Typst CLI 编译：

```bash
# 编译为 PDF
typst compile main.typ

# 监视模式（自动重新编译）
typst watch main.typ
```

或者使用 VS Code 的 [Typst LSP](https://marketplace.visualstudio.com/items?itemName=nvarner.typst-lsp) 扩展进行实时预览。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

---

**提示**：如需自定义样式，建议先阅读 `template/config.typ` 了解配置选项，再根据需要修改。
