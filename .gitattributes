# 设置默认行为
* text=auto

# Typst 源文件
*.typ text eol=lf

# 文档文件
*.md text eol=lf

# 配置文件
*.json text eol=lf
*.yaml text eol=lf
*.yml text eol=lf

# 字体文件（二进制）
*.ttf binary
*.otf binary
*.woff binary
*.woff2 binary

# PDF 文件（二进制）
*.pdf binary

# 图片文件（二进制）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.svg binary
*.ico binary

# Git LFS（如果需要管理大文件，可以取消注释以下行）
# *.pdf filter=lfs diff=lfs merge=lfs -text
# fonts/*.ttf filter=lfs diff=lfs merge=lfs -text
# fonts/*.otf filter=lfs diff=lfs merge=lfs -text
